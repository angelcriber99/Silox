import { createClient } from '@supabase/supabase-js'; import * as dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); supabase.from('posiciones').select('*').then(r => console.log(r.data?.filter(d => d.tipo === 'Liquidez' || d.ticker === 'CASH' || d.nombre?.toLowerCase().includes('cash') || d.nombre?.toLowerCase().includes('liquidez'))));
